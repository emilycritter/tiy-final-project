<div class="container">
  <h1>Create Artist Profile</h1>
  <p>
    You are currently logged in as
    <strong><%= link_to @current_user.full_name, user_path(id: @current_user.id) %></strong>.<br>
    If you would like to create this <strong>Artist Profile</strong> under a different user,
    please <%= link_to "sign out", sign_out_path %> and login with the desired user credentials.
  </p>
  <%= render 'form' %>
</div>

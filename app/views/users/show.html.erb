<h1>Users#show</h1>
<p>Find me in app/views/users/show.html.erb</p>
<%= link_to "Edit Profile", edit_user_path(id: @user.id) %>

<% if @user.favorites %>
  <% @user.favorites.each do |favorite| %>
    <%= favorite.piece.title %>
  <% end %>
<% end %>
